# For running Vite directly: copy this file to .env in this directory.
# For Docker Compose, configure the .env in the repository root instead.
# Backend origin, without /api. An empty value uses the frontend's origin.
VITE_API_BASE_URL=http://localhost:5167
